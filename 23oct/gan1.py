# Generative Adversarial Network (GAN)

numbers = [float(line.strip()) for line in open('./23oct/data/random_numbers.txt', 'r')]
print('Mean:', sum(numbers) / len(numbers))
print('Min:', min(numbers))
print('Max:', max(numbers))


